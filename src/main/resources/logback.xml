<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{20} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JDA is fairly chatty on DEBUG; keep it at INFO -->
    <logger name="net.dv8tion" level="INFO"/>
    <!-- Our own logs at DEBUG so you can see every interaction the bot receives -->
    <logger name="com.poker" level="DEBUG"/>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
